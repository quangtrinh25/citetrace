{
  "name": "@citetrace/cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": { "citetrace": "./dist/main.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": { "@citetrace/core": "workspace:*" }
}

